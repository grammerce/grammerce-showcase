# Зависимости модулей, вошедших в эту выборку. Версии совпадают с основным
# репозиторием. Это не полный список зависимостей продукта: веб-слой, кабинет
# и витрина сюда не входят.

# ─── Ядро ────────────────────────────────────────────────────────────────────
fastapi==0.124.4
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.1

# ─── База данных ─────────────────────────────────────────────────────────────
sqlalchemy==2.0.25
aiosqlite==0.19.0
asyncpg==0.29.0

# ─── Telegram-боты ───────────────────────────────────────────────────────────
aiogram==3.3.0
redis[hiredis]>=5.0

# ─── HTTP-клиент внешних интеграций (платежи, POS, Didox) ────────────────────
httpx==0.27.0

# ─── Авторизация ─────────────────────────────────────────────────────────────
bcrypt>=4.0.0

# ─── Счета и ЭСФ ─────────────────────────────────────────────────────────────
reportlab>=4.0

# ─── AI-карточка товара: обработка изображений ───────────────────────────────
pillow>=10.0

# ─── Тесты ───────────────────────────────────────────────────────────────────
pytest==8.0.0
pytest-asyncio==0.21.1

# Удаление фона и генерация текста (rembg, onnxruntime, google-genai) вынесены
# в requirements-optional.txt: код импортирует их лениво, внутри функций, и без
# них модули спокойно импортируются, а тесты проходят. Это сотни мегабайт,
# которые нужны только при фактическом запуске AI-конвейера.
